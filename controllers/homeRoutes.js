const router = require("express").Router();
const { BlogPost, User, Comment } = require("../models");
const withAuth = require("../utils/auth");

//get all blog posts for the homepage (what can be viewed without login)

//dashboard routes here?

//login/sign up

//logout
